from ...models import Character

name = "Bob"

description = "Bob is another character in the Garden of Eden."

bob = Character(
    name=name,
    description=description,
    image="",
    voice=""
)
